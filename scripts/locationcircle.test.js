const locationcircle = require('./locationcircle');

test ('check',coordinate => {
    function isValid(coordinate){
        try {
            expect(91,181).tobe(false);
            done();
        } catch (error) {
            done(error);
            
        }
    }
    ocationCircle('fetch data', isValid());
});
test('checks the valid coords of a location', done => {
   function isValidLocation(location){
      try{
         expect().toBeNull();
      done();
      }catch(error){
      done(error);
   }
}
locationCircle('fetch data', isValidLocation());
});
test('checks the location is valid type or not', done => {
   function isValidType(loc){
      try{
         expect().toBeNull();
      done();
      }catch(error){
      done(error);
   }
}
locationCircle('fetch data', isValidType());
});
    

